version: '3.8'

services:
  # Simple whoami service that shows container info
  whoami:
    image: traefik/whoami
    container_name: hello-world-whoami
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - hello-network

networks:
  hello-network:
    driver: bridge
